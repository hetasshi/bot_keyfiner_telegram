# Укажите токен Telegram-бота. Это единственный обязательный параметр.
TELEGRAM_BOT_TOKEN=

# Остальные настройки уже имеют безопасные значения по умолчанию.
# Раскомментируйте нужные строки, только если хотите переопределить поведение.
# MAX_FILE_MB=70                # Лимит размера входного файла в мегабайтах
# ANALYSIS_CONCURRENCY=2        # Количество одновременных задач анализа
# KEYFINDER_TEMP=.temp          # Каталог для временных файлов
# SHOW_CLOSE_KEY=false          # Показывать пятую строку «Близкий вариант»

# --- Продвинутые параметры ритм-анализа (опционально) ---
# BPM_MIN=60                    # Предпочтительный минимум темпа
# BPM_MAX=180                   # Предпочтительный максимум темпа
# BPM_DOUBLE_SWITCH=160         # Порог, после которого отдаём половинный темп
# BPM_HALF_SWITCH=70            # Порог, ниже которого можем удвоить темп
# BPM_CANDIDATE_TOLERANCE=1.5   # Допустимое расхождение кандидатов BPM
# BPM_SUPPORT_RATIO=0.9         # Насколько сильной должна быть поддержка альтернативы
